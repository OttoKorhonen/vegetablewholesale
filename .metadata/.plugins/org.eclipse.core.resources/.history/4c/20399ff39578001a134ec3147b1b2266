<!DOCTYPE HTML>
<html xmlns:th="http://www.thymeleaf.org"  xmlns:sec="http://www.thymeleaf.org/thymeleaf-extras-springsecurity5">
<head>
<title>Product list</title>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
 <link type="text/css" rel="stylesheet" />
</head>
<body>
<div class="col-md-4">
	<h1>Products</h1>
	</div>
	<div class="col-md-4">
		<h3 th:inline="text">Welcome [[${#httpServletRequest.remoteUser}]]</h3><!-- Tällä saadaan sisäänkirjautuneen käyttäjän nimi näkyviin -->
	</div>
	<div class="col-md-4" style="padding:20px 0">
		<form th:action="@{/logout}" method="post">
			<input type="submit" value="Sign Out" sec:authorize="hasAuthority('ADMIN')"/>
		</form>
		<form th:action="@{/login}" method="post">
			<input type="submit" value="Sign in" sec:authorize="hasRole('USER')"/>
		</form>
	</div>
	<table>
		<tr>
			<th>Name:</th>
			<th>Cultivar:</th>
			<th>Description:</th>
			<th>Producer:</th>
			<th>Country:</th>
			<th>Price:</th>
			<th>Category</th>
			<th sec:authorize="hasRole('ADMIN')"> </th>
		</tr>

		<tr th:each="product : ${products}">
			<!--  <td th:text="${product.productId}"></td>-->
			<td th:text="${product.name}"></td>
			<td th:text="${product.cultivar}"></td>
			<td th:text="${product.description}"></td>
			<td th:text="${product.producer}"></td>
			<td th:text="${product.country}"></td>
			<td th:text="${product.price}"></td>
			<td th:text="${product.category.name}"></td>
			<td sec:authorize="hasAuthority('ADMIN')"><a th:href="@{/deleteproduct/{productId}(productId=${product.productId})}">Delete</a></td><!--"@{/delete/{id}(id=${student.id})} poistetaan käyttäj.  hasRole('ADMIN')" jos käyttäjän rooli on tämä näytä tää -->
			<td sec:authorize="hasAuthority('ADMIN')"><a th:href="@{/editproduct/{productId}(id=${product.productId})}">Edit</a></td>
		</tr>
	</table>
	<a sec:authorize="hasAuthority('ADMIN')" href="/addproduct">New Product</a>
</body>
</html>